MySQL Backup Script.
================================================

This script is connecting to MySQL server, executes mysqldump and saves dumpfile to specified directory.

config file path is <python3 lib directory>/site|dist-packages/mysqlbackup/config/backup.json

run following command to executes this scripts!(must be privileged user)
python3 <python3 lib directory>/dist-packages/mysqlbackup/local_backup.py